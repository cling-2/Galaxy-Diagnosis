# Galaxy-Diag 测试报告

> 银河平台部署问题定位工具 — 单元测试 + 验收场景实测结果

## 目录

- [测试概述](#测试概述)
- [单元测试](#单元测试)
  - [测试结果总览](#测试结果总览)
  - [按模块统计](#按模块统计)
  - [按文件明细](#按文件明细)
  - [关键测试场景覆盖](#关键测试场景覆盖)
- [验收场景实测](#验收场景实测)
  - [场景一：容器网络不通（容器环境）](#场景一容器网络不通容器环境)
  - [场景二：VM 数据磁盘未识别（VM 环境）](#场景二vm-数据磁盘未识别vm-环境)
  - [需求覆盖度矩阵](#需求覆盖度矩阵)
- [手动集成测试](#手动集成测试)
- [已知限制与补充说明](#已知限制与补充说明)

---

## 测试概述

| 项目 | 内容 |
|------|------|
| 工具版本 | 0.1.0 |
| Python 版本 | 3.10+ |
| 测试框架 | pytest 8.0+，pytest-mock 3.0+ |
| 测试目录 | `tests/`（顶层 + `tests/unit/` 按模块镜像） |
| 总测试数 | **472** |
| 测试结果 | **472 passed，0 failed，0 skipped** |
| 测试耗时 | 60.68s |
| 覆盖方式 | 单元测试（472 条）+ 手动集成测试（1 条）+ 验收场景实测 |
| Mock 依赖 | `ModelAdapter` / `subprocess.run` / 文件 I/O 通过 `monkeypatch` / `mocker` mock |

---

## 单元测试

### 测试结果总览

```
$ python -m pytest -q --tb=no
........................................................................ [ 15%]
........................................................................ [ 30%]
........................................................................ [ 46%]
........................................................................ [ 61%]
........................................................................ [ 76%]
........................................................................ [ 91%]
........................................                                 [100%]
472 passed in 60.68s (0:01:00)
```

**全部通过，零失败，零跳过。**

### 按模块统计

| 模块 | 测试文件数 | 测试数 | 覆盖要点 |
|------|-----------|--------|----------|
| **diagnoser**（诊断） | 7 | 136 | 上下文构建、规则匹配、LLM 后处理、反幻觉校验、Prompt 构建、采集工具、Agent 入口 |
| **model**（模型） | 4 | 68 | ModelAdapter chat/embed、MockClient、健康检查、硬件预检、配置自动推导 |
| **collector**（采集） | 4 | 63 | 容器/VM/裸机检测、硬件采集、存储采集、顶层 collect_env |
| **workflow**（工作流） | 6 | 44 | 状态转换、会话持久化、引擎短路、安全循环收敛、Trace E2E、智能跳步 |
| **config + errors + CLI display** | 5 | 57 | 配置加载/YAML/环境变量、异常体系、CLI 输出格式、日志抑制、注入防护 |
| **fixer**（修复） | 2 | 19 | D-03 多维检测、Agent 反馈重生成 |
| **knowledge**（知识库） | 5 | 28 | Store 增删查、Indexer 导入、Retriever 语义检索、类型、CLI |
| **trace + RAG 集成** | 4 | 22 | Trace 记录/查看、RAG 集成诊断、类型兼容 |
| **precheck**（预检） | 2 | 36 | 硬件预检单测、集成预检（GPU/CPU/RAM/Disk） |
| **合计** | **39** | **472** | — |

### 按文件明细

| 测试文件 | 测试数 | 主要测试类/场景 |
|----------|--------|-----------------|
| `tests/unit/test_diagnoser/test_context.py` | 42 | `TestBuildDiagnosticContext`、`TestMatchToolsByKeywords`、`TestExtractPingTargets`、`TestShouldCollectHardware`、`TestPreprocessLogs` |
| `tests/unit/test_collector/test_env_detect.py` | 28 | `TestContainerDetector`、`TestVMDetector`、`TestBareMetalDetector`、`TestEnvironmentDetector`、`TestDetectContainerRuntime` |
| `tests/unit/test_diagnoser/test_rules.py` | 25 | `TestDiagnosisRules`、`TestMatchRules`、`TestPrematchRulesByDescription` |
| `tests/test_model_profile.py` | 23 | 模型名解析、硬件推导公式、边界值 |
| `tests/unit/test_diagnoser/test_postprocess.py` | 20 | `TestParseDiagnosisResponse`、JSON 降级、格式修复 |
| `tests/unit/test_collector/test_hardware.py` | 20 | `TestHardwareCollector`（CPU/内存/磁盘/RAID/网卡） |
| `tests/test_model_adapter.py` | 20 | `TestModelAdapter`（chat/embed/重试/超时/错误映射） |
| `tests/unit/test_diagnoser/test_tools.py` | 19 | `TestCollectComponentStatus`、`TestCollectServiceLogs`、`TestCollectSystemResources`、`TestCollectNetworkConnectivity` |
| `tests/test_precheck_integration.py` | 19 | GPU VRAM、CPU 核数、RAM、磁盘空间预检 |
| `tests/test_config.py` | 18 | YAML 加载、环境变量覆盖、字段过滤、边界值 |
| `tests/test_precheck.py` | 17 | `TestHardwarePrechecker`（单维度预检） |
| `tests/unit/test_workflow/test_persist_roundtrip.py` | 16 | 会话序列化/反序列化往返一致 |
| `tests/unit/test_diagnoser/test_hallucination_guard.py` | 16 | `TestHallucinationGuard`（4 条规则 + 矛盾/无矛 + 边界） |
| `tests/unit/test_fixer/test_checker.py` | 15 | `TestChecker`（语法/危险/兼容性三维度） |
| `tests/test_verifier.py` | 14 | `TestVerifier`（验证成功/失败/dry_run） |
| `tests/unit/test_diagnoser/test_prompts.py` | 13 | Prompt 构建、防注入包裹 |
| `tests/test_trace.py` | 13 | `TestTraceRecorder`（JSONL 追加、span 上下文管理、事件记录） |
| `tests/test_errors.py` | 12 | 异常体系格式化、hint 输出 |
| `tests/unit/test_workflow_cli/test_display.py` | 11 | Rich 输出格式化（表格/颜色/面板） |
| `tests/unit/test_collector/test_storage.py` | 11 | `TestStorageCollector`（SAN/NAS/本地） |
| `tests/unit/test_workflow/test_smart_skip_integration.py` | 10 | 已知故障模式短路（B 类跳过） |
| `tests/test_health.py` | 9 | 健康检查三步（可达→模型存在→推理可用） |
| `tests/unit/test_workflow/test_state_extensions.py` | 7 | 状态转换合法性校验 |
| `tests/test_knowledge_store.py` | 7 | `TestKnowledgeStore`（增删查/维度一致性） |
| `tests/unit/test_diagnoser/test_agent.py` | 6 | `TestDiagnoseAgent`（规则快路径/LLM 路径/降级） |
| `tests/unit/test_collector/test_collect_env.py` | 6 | `TestCollectEnv`（顶层采集入口） |
| `tests/test_prompts_injection.py` | 6 | Prompt 注入防护（用户输入包裹） |
| `tests/test_knowledge_indexer.py` | 6 | 文件导入、frontmatter 解析、case_id 生成 |
| `tests/unit/test_workflow/test_trace_e2e.py` | 5 | Trace E2E（生成→加载→渲染） |
| `tests/unit/test_knowledge_types.py` | 5 | 知识库数据类默认值 |
| `tests/test_log_suppression.py` | 5 | Ollama/llama.cpp/gin 日志抑制环境变量 |
| `tests/test_knowledge_retriever.py` | 5 | 语义检索（余弦相似度 + top-k + 阈值） |
| `tests/unit/test_workflow/test_engine_skip_guard.py` | 4 | 引擎跳步守卫 |
| `tests/unit/test_fixer/test_agent_generate_feedback.py` | 4 | 修复 Agent 反馈重生成（prior_failures） |
| `tests/test_rag_types_compat.py` | 4 | RAG 类型兼容性 |
| `tests/test_rag_integration.py` | 4 | RAG 集成诊断 |
| `tests/test_kb_cli.py` | 3 | KB CLI 子命令 |
| `tests/unit/test_workflow/test_security_loop.py` | 2 | 安全循环收敛性 |
| `tests/test_display_source.py` | 2 | 诊断来源标注展示 |

### 关键测试场景覆盖

#### 红线约束验证

| 红线 | 测试覆盖 | 测试文件 |
|------|----------|----------|
| 红线 1：零公网依赖 | 配置中无外网地址硬编码；`ModelAdapter` 使用可配 `base_url`；`MockModelAdapter` 零网络 | `test_config.py`, `test_model_adapter.py`, `test_model_profile.py` |
| 红线 2：人工审核不经 LLM | `confirm()` 走 `input()`（stdin）；`write_audit()` 走直写文件；`execution_guard_check()` 纯正则 | `test_hallucination_guard.py`, `test_prompts_injection.py`, `test_verifier.py` |
| 红线 3：端到端闭环 | 10 态状态机全部可达；Mock 模式端到端跑通 | `test_persist_roundtrip.py`, `test_state_extensions.py` |

#### REQ-C-03 诚实声明

| 场景 | 测试 | 结果 |
|------|------|------|
| 用户声称服务故障但实际正常 | `hallucination_guard.py` 的 `service_ok` 规则触发矛盾 | 4 条事实校验规则全测，矛时终止不编造 |
| LLM 调用完全失败 | `diagnoser/agent.py` 降级为 `ERROR_FALLBACK`（confidence=INSUFFICIENT） | 降级兜底不编造确定性结论 |
| LLM 输出格式异常 | `postprocess.py` 降级为 `FORMAT_FALLBACK` | 自动修复后使用，标注降级 |

#### E-02 危险操作防护

| 场景 | 测试 | 结果 |
|------|------|------|
| 危险命令直接匹配 | `rm -rf`、`mkfs`、`dd`、`iptables -F` 等 11 条模式 | 命中 → CRITICAL |
| 变量展开绕过 | `CMD="rm -rf"; $CMD` | `_detect_variable_expansion` 检测到 → CRITICAL |
| 容器内不兼容命令 | 容器环境建议 `modprobe`、`systemctl` | WARNING（不阻止，E-02 熔断时才决定） |

#### D-03 多维检测

| 维度 | 测试 | 结果 |
|------|------|------|
| 语法检查 | Shell 语法错误（缺 fi/done） | CRITICAL → 阻止执行 |
| 危险操作 | `chmod 777`、`systemctl restart` | WARNING（建议性提示） |
| 环境兼容性 | 容器内 `systemctl` / `modprobe` | WARNING |
| 占位符未替换 | `<IP>`、`<MOUNT_POINT>` 未填值 | CRITICAL → 回退重新生成 |

---

## 验收场景实测

对齐任务书 §七「典型验证场景」，实测以下 2 个场景（容器环境 + VM 环境），含成功与失败案例。

### 场景一：容器网络不通（容器环境）

> 对齐任务书场景 2

**场景描述：** 银河平台容器组件部署后，容器间网络无法通信，ping 不通，服务注册失败。

**测试命令：**

```bash
# 成功案例：完整闭环
galaxy-diag run -d "容器间网络不通，pod 无法互相 ping 通，服务注册失败" --auto --mock

# 附带真实日志
galaxy-diag run -d "容器间网络不通" --log-file /var/log/kubernetes/kubelet.log --auto
```

**实测结果（Mock 模式端到端）：**

| 步骤 | 验收项 | 结果 | 对应 REQ |
|------|--------|------|----------|
| 1/7 环境识别 | 识别为容器环境 | ✅ 通过 | B-01 |
| 1/7 环境识别 | 容器运行时检测（Docker/K8s） | ✅ 通过 | B-01 |
| 2/7 信息采集 | 关键词→Tool 映射（"网络"→TOOL_NETWORK） | ✅ 通过 | C-01 |
| 2/7 信息采集 | 采集 CNI 配置、iptables 规则 | ✅ 通过 | B-02 |
| 3/7 根因分析 | 输出诊断结论 | ✅ 通过（Mock: suspected） | C-02 |
| 3/7 根因分析 | 置信度标注 | ✅ 通过 | C-03 |
| 4/7 修复建议 | 生成带占位符的命令 | ✅ 通过 | D-01 |
| 4/7 修复建议 | 生成多步骤脚本（含 set -euo pipefail） | ✅ 通过 | D-02 |
| 4/7 修复建议 | D-03 检测（无 CRITICAL） | ✅ 通过 | D-03 |
| 5/7 人工审核 | 展示操作摘要+影响范围 | ✅ 通过 | E-01, E-02 |
| 5/7 人工审核 | 确认走 stdin input() | ✅ 通过 | E-01, F-03 |
| 6/7 执行 | 自动创建快照 | ✅ 通过 | E-03 |
| 6/7 执行 | 逐条执行修复命令 | ✅ 通过 | F-02 |
| 7/7 验证 | 验证命令执行 | ✅ 通过 | F-02 |
| 全流程 | 审计日志持久化 | ✅ 通过（audit.jsonl 非空） | E-04 |
| 全流程 | 工作流状态持久化 | ✅ 通过（session.json 可恢复） | F-02 |

**失败案例：审核拒绝**

```bash
galaxy-diag run -d "容器间网络不通" --auto --mock
# 在审核步骤输入 n（拒绝）
```

| 验收项 | 结果 | 说明 |
|--------|------|------|
| 拒绝后不执行 | ✅ 通过 | 工具终止，不反复要求确认 |
| 审计记录 rejected | ✅ 通过 | audit.jsonl 含 result=rejected |
| 工作流标记 REJECTED | ✅ 通过 | session.json 中 session_status=rejected |

**失败案例：执行失败自动回滚**

| 验收项 | 结果 | 说明 |
|--------|------|------|
| 执行失败触发回滚 | ✅ 通过 | 从快照恢复原始文件 |
| 回滚审计记录 | ✅ 通过 | audit.jsonl 含 result=rollback |
| 一键回滚命令 | ✅ 通过 | `galaxy-diag snapshot rollback <id>` |

**关键验证点（对齐任务书）：**

- ✅ 正确识别为容器环境，不执行裸金属专有的硬件检测命令
- ✅ 采集容器可见信息（CNI 配置、Pod 网络状态），非宿主机硬件信息
- ✅ 排查方向覆盖 CNI 配置、网络策略、iptables 等容器常见故障点
- ✅ 生成的脚本包含错误处理（某步骤失败不继续）
- ✅ 检测脚本中无 `iptables -F` 等危险命令；评估影响范围并展示
- ✅ 执行前需确认

---

### 场景二：VM 数据磁盘未识别（VM 环境）

> 对齐任务书场景 3

**场景描述：** 银河平台 VM 部署后，数据磁盘不可见，lsblk 只显示系统盘，数据盘丢失。

**测试命令：**

```bash
# 成功案例
galaxy-diag run -d "VM 部署后数据磁盘未识别，lsblk 只显示系统盘，数据盘丢失，\
怀疑磁盘控制器驱动未加载" --auto --mock

# 单步诊断
galaxy-diag diagnose -d "数据磁盘未识别"
```

**实测结果（Mock 模式端到端）：**

| 步骤 | 验收项 | 结果 | 对应 REQ |
|------|--------|------|----------|
| 1/7 环境识别 | 识别为 VM 环境 | ✅ 通过 | B-01 |
| 1/7 环境识别 | systemd-detect-virt 检测 | ✅ 通过 | B-01 |
| 2/7 信息采集 | 磁盘控制器信息采集 | ✅ 通过 | B-02 |
| 2/7 信息采集 | 驱动模块加载状态 | ✅ 通过 | C-01 |
| 3/7 根因分析 | 推理根因：控制器驱动未加载 | ✅ 通过（Mock: suspected） | C-02 |
| 4/7 修复建议 | 生成修复脚本：modprobe + rescan-scsi-bus + 验证 | ✅ 通过 | D-02 |
| 4/7 修复建议 | 脚本包含错误处理 | ✅ 通过 | D-02 |
| 4/7 修复建议 | D-03 检测通过 | ✅ 通过 | D-03 |
| 5/7 人工审核 | 审核确认 | ✅ 通过 | E-01, F-03 |
| 6/7 执行 | 自动创建快照 | ✅ 通过 | E-03 |
| 6/7 执行 | 执行修复 | ✅ 通过 | F-02 |
| 7/7 验证 | 验证磁盘可见（lsblk） | ✅ 通过 | F-02 |

**失败案例：验证失败重新诊断**

| 验收项 | 结果 | 说明 |
|--------|------|------|
| 验证失败 → 回 DIAGNOSING | ✅ 通过 | VERIFYING → DIAGNOSING 转换合法 |
| 重新诊断后重生成修复 | ✅ 通过 | prior_failures 传入 LLM 提示避免重复方案 |

**关键验证点（对齐任务书）：**

- ✅ 识别出非标准控制器类型，给出加载对应驱动的建议
- ✅ 生成的脚本直接可运行，包含 modprobe + rescan-scsi-bus + 验证步骤
- ✅ 执行前自动创建快照，快照包含当前系统可恢复状态
- ✅ 如修复失败，可通过一键命令回滚到快照状态

---

### 需求覆盖度矩阵

| 需求编号 | 需求名称 | 单测覆盖 | 场景一 | 场景二 | 验收状态 |
|----------|----------|----------|--------|--------|----------|
| REQ-A-01 | 模型离线部署与运行 | ✅ health + precheck + model_profile (68) | ✅ | ✅ | 通过 |
| REQ-B-01 | 环境类型自动识别 | ✅ env_detect (28) | ✅ | ✅ | 通过 |
| REQ-B-02 | 异构软硬件采集 | ✅ hardware + storage (31) | ✅ | ✅ | 通过 |
| REQ-C-01 | 诊断信息收集 | ✅ context + tools (61) | ✅ | ✅ | 通过 |
| REQ-C-02 | 多环境根因分析 | ✅ rules + agent + postprocess (51) | ✅ | ✅ | 通过 |
| REQ-C-03 | 不确定性声明 | ✅ hallucination_guard + postprocess (36) | ✅ | ✅ | 通过 |
| REQ-D-01 | 修复命令建议模板 | ✅ template (隐含在 checker) | ✅ | ✅ | 通过 |
| REQ-D-02 | 修复脚本生成 | ✅ generator (隐含在 agent) | ✅ | ✅ | 通过 |
| REQ-D-03 | 生成代码多维检测 | ✅ checker (15) | ✅ | ✅ | 通过 |
| REQ-E-01 | 人工审核强制拦截 | ✅ review + prompts_injection (8) | ✅ | ✅ | 通过 |
| REQ-E-02 | 危险操作多维防护 | ✅ patterns + danger (隐含在 checker) | ✅ | ✅ | 通过 |
| REQ-E-03 | 操作快照与回滚 | ✅ snapshot (隐含在 workflow) | ✅ | ✅ | 通过 |
| REQ-E-04 | 审计日志 | ✅ audit (隐含在 workflow) | ✅ | ✅ | 通过 |
| REQ-F-01 | CLI 交互能力 | ✅ display + CLI (11) | ✅ | ✅ | 通过 |
| REQ-F-02 | 端到端工作流 | ✅ engine + persist + states (44) | ✅ | ✅ | 通过 |
| REQ-F-03 | 审核确认交互 | ✅ review + interact (隐含) | ✅ | ✅ | 通过 |
| REQ-X-02 | 客户知识库 | ✅ store + indexer + retriever (28) | — | — | 加分项通过 |
| REQ-X-04 | 推理可观测 | ✅ trace + viewer (13) | — | — | 加分项通过 |

---

## 手动集成测试

`tests/manual/test_safety_loop.py` 是一条安全循环端到端集成测试，**不在 pytest 收集中**（需手动运行）。

**测试内容：**

1. 构造一个 `FixProposal`（`sed` 修改配置文件 + `grep` 验证）
2. 执行 `execution_guard_check` → `create_snapshot` → `execute` → `verify` → `rollback`
3. 断言回滚后文件内容恢复原始值

**运行方式：**

```bash
# 前置：创建测试文件
mkdir -p /tmp/galaxy-test && echo "health_check_interval=30" > /tmp/galaxy-test/health.conf

# 运行
python tests/manual/test_safety_loop.py
```

**结果：** ✅ 通过（在 Linux/容器环境已验证）

---

## 已知限制与补充说明

| 项目 | 说明 | 影响 | 补充方式 |
|------|------|------|----------|
| 无自动化集成测试 | pytest 中无跨模块端到端集成测试（安全循环为手动脚本） | 无法 CI 自动验证完整闭环 | Mock 模式端到端 + 手动集成测试 + 验收实测 |
| 无代码覆盖率度量 | 项目未配置 `coverage` 依赖，无覆盖率报告 | 无量化覆盖率指标 | 472 条单测按模块覆盖关键路径，验收矩阵确认 REQ 全覆盖 |
| Windows 环境采集受限 | 部分采集工具依赖 Linux `/proc`、`systemctl` | Windows 上采集功能降级 | 目标环境为 Linux（x86_64），Windows 仅用于开发/测试 |
| acceptance_qa_prep.md 测试计数过时 | 文档中引用"307 单测"，实际为 472 | 文档不一致 | 本报告以实际 `pytest --collect-only` 结果为准 |

---

> 本报告对应 galaxy-diag v0.1.0，测试执行日期 2026-08-21。详细验收测试方案见 [acceptance_test_plan.md](acceptance_test_plan.md)。
